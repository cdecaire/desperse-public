node_modules
.DS_Store
dist
dist-ssr
*.local
echoes-fee-payer.json
count.txt
.env
.nitro
.tanstack
echoes-dev
.wrangler
.output
.vinxi
todos.json
.vercel
.env*.local

# Internal scripts (sensitive admin/key operations)
scripts-internal/
docs-internal/

# Claude Code local settings (may contain permissions with sensitive data)
.claude/settings.local.json
nul

# TanStack Router generated route tree — regenerated by tanstackStart() on dev/build.
# Untracked to avoid drift when routes are added in environments that can't run codegen.
src/routeTree.gen.ts
