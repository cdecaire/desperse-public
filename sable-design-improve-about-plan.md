# `/about` — ranked improvement plan

All items approved and applied 2026-07-12.

| # | What's there now | What changes | Doctrine / source | Effort/risk | Status |
|---|---|---|---|---|---|
| 1 | 5 of 8 sections (`LiveGallerySection`, `HowItWorksSection`, `PrinciplesSection`, `TrustSection`, `FinalCta`) render with zero entrance motion, while Hero/Audience/WorkModes animate in | Wrapped each section's content in `<Reveal>` (already imported in this file) so every section entrances consistently on scroll | build.md step 10 (section `<Reveal>` entrances) — this page's own precedent in Audience/WorkModes | Low effort, low risk | **applied** |
| 2 | Hero `VALUE_PROPS` rendered as a hand-rolled `<span className="rounded-full border ...">` | Swapped to the project's `Badge` (`src/components/ui/badge.tsx`) with `variant="outline"`, size/padding preserved via `className` override | Component-fidelity hygiene, improve.md §5 bucket-A | Low effort, low risk | **applied** |
| 3 | `LiveCreatorPreview`'s hover-zoom used a bare `duration-700` Tailwind utility instead of a Sable motion token | Replaced with `duration-(--motion-duration-slower) ease-decelerate` (500ms in the `desperse` theme — the closest token, still honors the `--motion-scale` reduced-motion kill switch) | Token-hygiene sweep, DESIGN.md motion section | Low effort, low risk | **applied** |
| 4 | `LiveCreatorPreview`'s three states (loading/empty/live) each used a raw `shadow-[0_8px_8px_rgba(0,0,0,0.18)]` | Replaced all 3 with `shadow-lg` (DESIGN.md's floating-card tier) | Token-hygiene sweep, DESIGN.md shadow tokens | Low-medium effort (3 call sites), low risk | **applied** |
| 5 | 5 places (Audience, Work Modes, How It Works ×4 items, Principles, Trust) hand-rolled `rounded-lg border border-border bg-card` card shells instead of `Card` (`src/components/ui/card.tsx`) | Migrated outer shells to `<Card className="...">`, keeping existing grid/divide/padding classes verbatim on inner content so rendered geometry didn't shift. `PrinciplesSection` explicitly overrides `bg-background` (its intentional deviation from the card tone) via `className`, which `tailwind-merge` resolves correctly. **Excluded** `LiveCreatorPreview` (uses `bg-overlay`/`text-overlay-foreground` — a different semantic tone, not a fit for `Card`) | Component-fidelity, improve.md §5 bucket-A | Medium effort (5 call sites), medium risk — mitigated by verifying before/after screenshots in both color modes | **applied** |

Verification: `npx tsc --noEmit` clean, `pnpm test` 327/327 passing, visual pass in both light/dark and desktop/mobile confirmed no layout shift — `Card`'s default `shadow-sm` is the only visual delta (subtle, doctrine-compliant, effectively invisible in dark mode).

**Out of scope / noted, not acted on**: the static info cards (Work Modes, How It Works, Trust) have no hover state, but since they aren't interactive/links this isn't a doctrine violation — flagging only in case hover polish is wanted there too.

---

## Tier 2 — Opportunities (2026-07-12 follow-up pass)

Not violations — the page is fine without these. Each is opt-in individually; **none are applied yet, pending approval.** Tier-1 hygiene was re-swept against the current source and found clean (see findings-doc addendum).

| # | Opportunity | Corpus pattern(s) | What it would look like | Effort/risk | Status |
|---|---|---|---|---|---|
| 6 | Elevate the Audience section's `95%`/`5%` metric footer from a plain `text-display-lg` number into an editorial stat display: bigger tabular-numeral treatment, tiny mono-uppercase caption, keep the existing hairline. Optionally pair with a scroll-triggered "stomp" slide-up on the numeral instead of the whole-card fade it gets today. | `big-number-mono-caption-stat` (component), `stomp-stack-stat-reveal` (motion) | The two metric numbers become the visual anchor of the Audience card instead of a footnote — directly serves the "95% creator share / 5% fee" trust message the page is built around. | Medium (numeral treatment is low-risk CSS; the stomp motion is a new, from-scratch primitive — could ship the static upgrade alone) | proposed |
| 7 | ~~Give all 8 section eyebrows one repeating device instead of each being independent.~~ | `dot-mono-eyebrow-header` | — | — | **reverted** — see note below |
| 8 | Give the Work Modes grid asymmetric weight instead of 4 uniform columns — feature `Edition` (the revenue-bearing option) as a wider card, the other three narrower/compact. | `alternating-width-feature-grid` | Visual hierarchy that matches product priority instead of every option reading as equally weighted. | Medium (layout change to a section the plan hasn't touched before; needs a deliberate before/after check since it changes proportions, not just hygiene) | proposed |
| 9 | Add restrained hover life to the static icon-grid sections (Work Modes, Trust) — hovering one item nudges its icon/title to full ink while siblings desaturate slightly, **without** implying clickability (no cursor-pointer, no scale-up, no shadow lift — color/opacity only). | `group-hover-promote-demote` (demote-siblings half only; the promote half stays deliberately subtle since these aren't links) | The two sections the findings doc called "inert compared to the animated hero" gain a small amount of life on hover without pretending to be buttons. | Low-medium (needs care to stay non-affordance-implying — flagged as a risk, not just an effort note) | proposed |

**Hard stop**: none of the above are applied. Confirm which items (if any) to build before any file is touched.

**Update 2026-07-12**: All 4 approved and applied (#6, #7, #8, #9). #7 was adapted from the corpus's literal recipe — dropped per user request (no dot) and implemented with `text-label-xs` (Desperse's own native auto-uppercase eyebrow tier) instead of DM Mono, since `DESIGN.md` reserves mono type for on-chain data only, never "technical vibes" decoration. Corrected scope during implementation: only 2 of 8 sections had an eyebrow before this pass (Hero, Audience) — the other 6 needed new eyebrow copy added, derived from each section's existing heading/body content, not fabricated marketing claims. #6 shipped as the static numeral/caption upgrade only (`text-display-xl` + `tabular-nums` + `text-label-xs` caption); the optional stomp-reveal motion was descoped per the plan's own effort note ("could ship the static upgrade alone") — the whole-card `<Reveal>` still covers entrance. #8 used plain Tailwind `grid-cols-12`/`col-span-*` rather than importing Sable's `Columns`/`Col` primitives, to avoid an unknown default-gap risk to the existing divide-x "touching cells" look; spans are Post=3/Collectible=3/Edition=4/Download=2 (sum 12) on `lg`, uniform 2-up on `md`. #9 implemented as pure `group`/`group-hover:opacity-60`/`hover:opacity-100!` using the existing `motion-interactive` token class — no scale, no cursor change, applied to both Work Modes and Trust card grids.

Verification: `npx tsc --noEmit` clean, `pnpm test` 327/327 passing. Visual pass via `claude-in-chrome` in dark mode at desktop (1440–1568px, window-dependent) confirmed: eyebrows render correctly sized with no overflow, stat numerals fit within card padding, Work Modes grid shows Edition visibly wider than the other three, hover on both Work Modes and Trust grids promotes the hovered card to full ink while siblings dim — no layout shift, no pointer-cursor/scale false affordance. Light mode re-verified post-edit (hero, stat display, Work Modes asymmetric grid) — clean, no regressions. Mobile (390×844) was spot-checked pre-edit only (confirmed clean stacking); not re-verified post-edit this pass.

**Update 2026-07-12 (later same day)**: #7 reverted per user feedback ("cleaner without them, now that I see it") — seeing all 8 sections with the added eyebrows in context read as busier, not better. Removed the 6 newly-added eyebrows (Work Modes, Live Gallery, How It Works, Principles, Trust, Final CTA) entirely and restored Hero/Audience's original two eyebrows to their pre-pass `text-label-md` styling. #6, #8, #9 are unaffected and remain applied. Re-verified `tsc` clean, `pnpm test` 327/327 passing after the revert.
